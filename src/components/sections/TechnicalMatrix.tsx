import {Box,Grid,Typography} from '@mui/material';import {Check} from '@mui/icons-material';export type MatrixItem={title:string;description:string};export default function TechnicalMatrix({items,columns=3,dark=false,checks=false}:{items:MatrixItem[];columns?:3|4;dark?:boolean;checks?:boolean}){return <Box sx={{borderTop:'1px solid',borderLeft:'1px solid',borderColor:dark?'rgba(255,255,255,.14)':'#e0e0e0'}}><Grid container>{items.map((item,i)=><Grid item xs={12} sm={6} md={12/columns} key={item.title} sx={{minHeight:205,p:4,borderRight:'1px solid',borderBottom:'1px solid',borderColor:dark?'rgba(255,255,255,.14)':'#e0e0e0'}}><Typography color={checks?'#d81159':'#00b4d8'} mb={2}>{checks?<Check/>:String(i+1).padStart(2,'0')}</Typography><Typography variant="h5" fontWeight={800} mb={1}>{item.title}</Typography><Typography color={dark?'#aeb9c8':'text.secondary'} lineHeight={1.7}>{item.description}</Typography></Grid>)}</Grid></Box>}
