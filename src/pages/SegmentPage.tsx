import {Box,Container,Typography} from '@mui/material';import HeroSection from '../components/sections/HeroSection';import TechnicalMatrix, {MatrixItem} from '../components/sections/TechnicalMatrix';import DynamicQuoteForm from '../components/sections/DynamicQuoteForm';export default function SegmentPage({tag,title,intro,items,formTitle,image}:{tag:string;title:string;intro:string;items:MatrixItem[];formTitle:string;image:string}){return <><HeroSection compact fullHeight image={image} tag={tag} title={title} body={intro}/><Box sx={{py:{xs:9,md:13}}}><Container><Typography variant="h2" sx={{fontSize:{xs:38,md:56},maxWidth:820,mb:6}}>Inteligência aplicada ao seu contexto.</Typography><TechnicalMatrix items={items} checks/></Container></Box><DynamicQuoteForm title={formTitle}/></>}
